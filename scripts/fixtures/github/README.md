# Recorded GitHub GraphQL fixtures

Responses to `MERGED_PRS_QUERY` in `../../seed-github.ts`, trimmed to the fields
the seeder reads. Keyed by `owner/name` plus the `after` cursor the request
carried, so a fixture run reproduces real pagination.

Recorded against a window of `2026-08-01..2026-08-13`. No tokens or private data
are involved — every field here is public.
