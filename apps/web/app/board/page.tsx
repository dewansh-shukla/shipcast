/**
 * TICKET C1 — leaderboard.
 *
 * Two ranked lists on one page. The open board carries everyone, seeded from
 * public GitHub data with no install required. The orchestrator board carries
 * weighted scores and only exists for builders who connected the collector —
 * seeded rows show a locked score cell, and that empty cell is the entire
 * conversion pitch.
 *
 * Rows expand into the arithmetic that produced the score. An opaque number
 * nobody can audit is worth less than a smaller number they can.
 */
export default function BoardPage() {
  return (
    <main>
      <h1>Board</h1>
      <p>TICKET C1: not implemented</p>
    </main>
  );
}
